<div class="sheet-section item-actions">
	<div class="item-actions__head">
		<span class="field__label">Actions</span>
		{{#if isEditable}}
			<button
				type="button"
				class="array-action-btn"
				data-action="add-item-action"
				{{#unless canAddItemAction}}disabled{{/unless}}
				title="Ajouter"
			>
				<i class="fa-light fa-square-plus"></i>
			</button>
		{{/if}}
	</div>

	{{#if itemActions.length}}
		<div class="item-actions__list">
			{{#each itemActions as |itemAction|}}
				<div class="item-actions__row panel-section">
					<div class="item-actions__icon">
						<img
							class="item-actions__image"
							src="{{@root.itemImage}}"
							alt="{{@root.item.name}}"
						/>
						<span class="item-actions__shorthand">{{itemAction.displayShorthand}}</span>
					</div>

					<div class="item-actions__main">
						<div class="item-actions__line item-actions__line--title">
							<div class="item-actions__title-main">
								<div class="item-actions__cost-summary">
									{{#each itemAction.resourceSummary.options as |costOption|}}
										<span class="item-actions__cost-option">
											{{#each costOption.tokens as |token|}}
												<span
													class="item-actions__cost-token item-actions__cost-token--{{token.colorClass}}"
													title="{{token.title}}"
												>
													{{#if token.showAmount}}
														<span class="item-actions__cost-token-amount">{{token.amountLabel}}</span>
													{{/if}}
													<i class="{{token.iconClass}}"></i>
												</span>
											{{/each}}
											{{#if costOption.hasCondition}}
												<span class="item-actions__cost-condition-mark">*</span>
											{{/if}}
										</span>
										{{#unless @last}}
											<span class="item-actions__cost-option-sep">/</span>
										{{/unless}}
									{{/each}}
									{{#each itemAction.resourceSummary.additions as |token|}}
										<span class="item-actions__cost-option-sep">+</span>
										<span
											class="item-actions__cost-token item-actions__cost-token--{{token.colorClass}}"
											title="{{token.title}}"
										>
											{{#if token.showAmount}}
												<span class="item-actions__cost-token-amount">{{token.amountLabel}}</span>
											{{/if}}
											<i class="{{token.iconClass}}"></i>
										</span>
									{{/each}}
								</div>
								{{#if itemAction.resourceSummary.note}}
									<span
										class="item-actions__cost-note"
										title="{{itemAction.resourceSummary.note}}"
									>
										(<i class="fa-light fa-note-sticky"></i>)
									</span>
								{{/if}}
								<span class="item-actions__name-label">{{itemAction.displayName}}</span>
							</div>
							<div class="item-actions__summary-stack">
								{{#if itemAction.summaryTop}}
									<span class="item-actions__summary item-actions__summary--top">
										{{itemAction.summaryTop}}
									</span>
								{{/if}}
								{{#if itemAction.summaryBottom}}
									<span class="item-actions__summary item-actions__summary--bottom">
										{{itemAction.summaryBottom}}
									</span>
								{{/if}}
							</div>
						</div>
						{{#if itemAction.secondarySummary}}
							<div class="item-actions__line item-actions__line--meta">
								<span class="item-actions__secondary">{{itemAction.secondarySummary}}</span>
							</div>
						{{/if}}
					</div>

					{{#if @root.isEditable}}
						<div class="item-actions__buttons">
							<button
								type="button"
								class="array-action-btn"
								data-action="edit-item-action"
								data-index="{{itemAction.index}}"
								title="Modifier"
							>
								<i class="fa-light fa-pen-to-square"></i>
							</button>
							<button
								type="button"
								class="array-action-btn"
								data-action="remove-item-action"
								data-index="{{itemAction.index}}"
								title="Supprimer"
							>
								<i class="fa-light fa-square-minus"></i>
							</button>
						</div>
					{{/if}}
				</div>
			{{/each}}
		</div>
	{{else}}
		<p class="hint item-actions__empty">Aucune action.</p>
	{{/if}}
</div>
